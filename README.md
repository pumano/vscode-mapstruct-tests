# vscode-mapstruct-tests

repo for reproduce issue with unavailable impl for mapstruct mappers when execute via vscode test runner

## run tests

`gradle clean test`

## run test in vscode

"Run Test" via method annotation
